<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{{movie.title}} - Movie Details</title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
		integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
	<link rel="stylesheet" href="/fontawesome-free-6.7.1-web/css/all.css">
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
		integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
		crossorigin="anonymous"></script>
	<link rel="stylesheet" href="/css/style.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.7/handlebars.min.js"></script>
</head>
<body>
    <h1>Movie Library</h1>
    <div id="movie-list">
        {{#each movies}}
            <div class="movie-item" data-id="{{id}}">
                <img src="{{poster}}" alt="{{title}} Poster">
                <h3>{{title}}</h3>
                <p>{{year}}</p>
            </div>
        {{/each}}
    </div>
    <div id="movie-details"></div>

    <template id="movie-details-template">
        <h2>{{title}}</h2>
        <img src="{{poster}}" alt="{{title}} Poster" style="float: right; max-width: 300px; margin-left: 20px;">
        <p><strong>Year:</strong> {{year}}</p>
        <p><strong>Director:</strong> {{director}}</p>
        <p><strong>Plot:</strong> {{plot}}</p>
        <h3>Cast</h3>
        <ul>
            {{#each cast}}
                <li>{{this}}</li>
            {{/each}}
        </ul>
        {{#if ratings}}
            <h3>Ratings</h3>
            <ul>
                {{#each ratings}}
                    <li><strong>{{source}}:</strong> {{value}}</li>
                {{/each}}
            </ul>
        {{/if}}
    </template>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.7/handlebars.min.js"></script>
    <script src="/film.js"></script>
</body>
</html>